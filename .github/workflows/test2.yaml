name: checkout
on:
    push:
      branches:
        - main
      
jobs:
  terraforms:
    runs-on: ubuntu-latest
    steps:
      - name: echo "Running Terraform Commands"
        run: echo "hello world"
      
      - name: clone code
        uses: actions/checkout@v4

      - name: List
        run: ls
            
    